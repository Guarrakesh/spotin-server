/* eslint-disable */
import React from 'react';
import {
  Show,
  SimpleShowLayout,
  ReferenceField,
  TextField,
  RichTextField,
  DateField} from 'react-admin';

const BroadcastReviewShow = (props) => (
  <Show {...props}>

  </Show>
);

export default BroadcastReviewShow;
